import React from 'react';
import styled from 'styled-components';

const TitleText = styled.div`
    letter-spacing: 5px;
    font-size: 2rem;
    color: blueviolet;
`;

const Title = () => {
    return <TitleText>TODO</TitleText>;
};

export default Title;
